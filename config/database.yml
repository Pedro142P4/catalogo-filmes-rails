# config/database.yml

default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: catalogo_filmes_rails_development

  # 👇👇👇 É AQUI QUE VOCÊ PRECISA EDITAR 👇👇👇

  # Coloque seu usuário do PostgreSQL (o padrão é 'postgres')
  username: postgres

  # Coloque a SENHA que você definiu ao instalar o PostgreSQL
  password: Mineple1

  # Essas linhas ajudam a garantir a conexão
  host: localhost
  port: 5432

  # 👆👆👆 FIM DA EDIÇÃO 👆👆👆


test:
  <<: *default
  database: catalogo_filmes_rails_test


  username: postgres
  password: Mineple1
  host: localhost
  port: 5432


production:
  primary:
    <<: *default
    database: <%= ENV.fetch("DATABASE_URL") %>
  cable:
    <<: *default
    database: <%= ENV.fetch("DATABASE_URL") %>